# Gidrolock Modbus Scanner
ѕростой Modbus сканнер, заточенный под устройства Gidrolock.

 нопка "ѕодключитьс€" отправл€ет запрос на чтение шести Input Register'ов по адресу `200` (модель устройства/платы дл€ устройств Gidrolock). 

##  онфигурации и автоопрос
 онфигураци€ Ч это `.json` файл с описанием команд чтени€ дл€ конкретных моделей устройств. ѕоддерживает парсинг в UTF-8.

ѕол€ и возможные значени€:

```js
{
	// им€ шаблона/устройства
	"name" : "Gidrolock Standard Wi-Fi RS-485",

	// ќписание устройства
	"description" : "Smart valve controller unit with wired and wireless leak sensor support",

	// список данных, получаемых с устройства
	// кажда€ запись €вл€етс€ отдельным полем данных
	// и поддерживает опрос нескольких регистров, 
	// а также парсинг в стандартные типы данных (UTF-8, int и т.д.)
	"entries" : [
		{
			// им€ записи
			"name": "Modbus ID",

			// тип опрашиваемых регистров:
			// "coil", "discrete", "input", "holding"
			"registerType": "holding",

			// адрес стартового регистра
			// отсчет с 0
			"address": 128,

			// количество опрашиваемых регистров
			// значение по-умолчанию: 1
			"length": 1,

			// тип данных дл€ парсинга
			// поддерживаемые типы: bool, uint16, uint32, utf8
			// значение по умолчанию: uint16
			"dataType": "uint16",

			// опрашивать ли эти регистры повторно
			// при `false` опрашиваетс€ только в первый раз
			"readOnce": true
		}
	],

	// ”никальное дл€ устройства значение: модель устройства, верси€ прошивки, etc.
	"checkEntry": {
		"registerType": "input", 
		"address": 200,
		"length": 6,
		"dataType": "string",

		// ожидаемое значение при опросе этих регистров
		"expectedValue": "SWT485"
	}

}
```


### To-Do
1. ѕеребор всех `.json` шаблонов в папке дл€ автоопределени€ устройства
2. Ќастраиваемый цикличный опрос регистров
3. ѕоддержка Modbus TCP

